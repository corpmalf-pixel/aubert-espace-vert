export default function App() {
  return (
    <main className="shell">
      <section className="hero">
        <p className="eyebrow">Aubert Espace Vert</p>
        <h1>Base projet prête pour construire un site premium.</h1>
        <p className="lede">
          Structure React + Vite initialisée, dépôt préparé, configuration Vercel prête.
        </p>
      </section>
    </main>
  )
}
